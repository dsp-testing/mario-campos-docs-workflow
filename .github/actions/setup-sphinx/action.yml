name: Setup sphinx
description: Download and install Sphinx.

runs:
  using: composite
  steps:
  - name: Install sphinx
    run: sudo pip install --requirement "$GITHUB_ACTION_PATH/requirements.txt"
    shell: bash
